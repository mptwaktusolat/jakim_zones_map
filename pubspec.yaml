name: jakim_zones_map
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=2.19.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_map: ^3.1.0
  flutter_map_geojson: ^1.0.1
  geojson: ^1.0.0
  http: ^1.0.0
  latlong2: ^0.8.1
  maps_toolkit: ^2.0.1
  url_launcher: ^6.1.11

dependency_overrides:
  flutter_map: ^5.0.0
  latlong2: ^0.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
